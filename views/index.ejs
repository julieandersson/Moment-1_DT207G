<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moment 1 - DT207G</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <header>
        <ul>
            <li><a href="/">STARTSIDA</a></li>
            <li><a href="/create">LÄGG TILL KURS</a></li>
            <li><a href="/aboutsite">OM DENNA WEBBPLATS</a></li>
        </ul>
    </header>
    <main>
    <h1>Befintliga kurser</h1>
    <h2>Här finner du dina befintliga kurser...</h2>

    <% if(locals.rows.length > 0) { %>
        <% locals.rows.forEach((row) => { %>
            <article>
                <p>Kurskod:
                <%= row.coursecode %>
                </p>

                <p>Kursnamn:
                    <%= row.coursename %>
                </p>

                <p>Länk till kursplan:
                    <%= row.syllabus %>
                </p>

                <p>Progression:
                    <%= row.progression %>
                </p>
                <p class="posted">
                    Postad:
                    <%= row.created %>
                </p>

                <!--Knapp för att radera specifik kurs-->
                <p class="delete">
                    <a href="/delete/<%= row.id %>">Radera tillagd kurs</a>
                </p>

                <!--Knapp för att redigera/ändra specifik kurs-->
                <p class="edit">
                    <a href="/editcourse/<%= row.id %>">Ändra kursinformation</a>
                </p>

            </article>

      <% }); %>
    
    <% } %>

    </main><br>

    <footer>
        <p>Moment 1 - DT207G<br>
        Av Julie Andersson</p>
    </footer>
</body>
</html>